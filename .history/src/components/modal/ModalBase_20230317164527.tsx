import React from "react";
import { CSSTransition } from "react-transition-group";

type Props = {};

const ModalBase = (props: Props) => {
  return <CSSTransition></CSSTransition>;
};

export default ModalBase;
